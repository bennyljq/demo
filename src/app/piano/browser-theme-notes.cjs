const{chromium}=require(process.env.TEMP+'/piano-browser-tools/node_modules/playwright-core');
(async()=>{const b=await chromium.launch({executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe',headless:true});const p=await b.newPage();await p.goto('http://localhost:4200/piano');await p.selectOption('#piano-song','twinkle-theme');await p.waitForFunction(()=>{const c=ng.getComponent(document.querySelector('app-piano'));return c.playback.source()==='twinkle-theme'&&c.playback.status()==='ready'});const data=await p.evaluate(()=>{const s=ng.getComponent(document.querySelector('app-piano')).playback.score();return s.measures.filter(m=>m.occurrence===1).map(m=>({m:m.number,notes:s.soundingNotes.filter(n=>n.staff===1&&n.voice==='1'&&n.onsetQuarter>=m.quarter&&n.onsetQuarter<m.quarter+m.durationQuarter).map(n=>[+(n.onsetQuarter-m.quarter).toFixed(3),n.pitch,+(n.durationQuarter).toFixed(3)])}))});console.log(JSON.stringify(data));await b.close()})().catch(e=>{console.error(e);process.exit(1)});
