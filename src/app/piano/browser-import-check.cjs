const{chromium}=require(process.env.TEMP+'/piano-browser-tools/node_modules/playwright-core');(async()=>{let b=await chromium.launch({executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe',headless:true});let p=await b.newPage();await p.goto('http://localhost:4200/piano');await p.waitForFunction(()=>ng.getComponent(document.querySelector('app-piano')).playback.status()==='ready');for(let id of ['twinkle-theme','twinkle-variation-01','twinkle-variation-11','twinkle-variation-12','12-variations-of-twinkle-twinkle-little-star']){await p.selectOption('#piano-song',id);await p.waitForFunction(id=>{let x=ng.getComponent(document.querySelector('app-piano')).playback;return x.source()===id&&x.status()!=='loading'},id,{timeout:30000});console.log(id,await p.evaluate(()=>{let x=ng.getComponent(document.querySelector('app-piano')).playback;return{x:x.status(),e:x.error(),m:x.score()?.measureCount,d:x.duration()}}))}await b.close()})().catch(e=>{console.error(e);process.exit(1)});
